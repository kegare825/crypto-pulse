crypto_pulse:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'pulse') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'pulse') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'cryptopulse') }}"
      schema: public
      threads: 2
